import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Política de Privacidad — Du Labs",
  description: "Política de privacidad de Du Labs y sus productos (Du Life, Du Academy, Du IA Business).",
};

export default function PrivacidadLayout({ children }: { children: React.ReactNode }) {
  return children;
}
